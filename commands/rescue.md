---
description: Align an existing, misaligned codebase — invoke the codebase-rescue skill.
---

Use the **codebase-rescue** skill (`skills/codebase-rescue/SKILL.md`). Start at Phase 1
(comprehension + finding) and build the pinned map — do NOT jump straight to fixes. If the scope
is unclear, pick a mode with me first: `rescue` (default) · `align` · `audit` · `resume`.

$ARGUMENTS
