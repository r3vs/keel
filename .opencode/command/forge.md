---
description: Build a new project aligned from the first commit — invoke the greenfield-forge skill.
agent: researcher
---

Use the **greenfield-forge** skill (`skills/greenfield-forge/SKILL.md`). Start at Phase 1 (frame:
testable outcomes + open decisions + the threat-model pass) — do NOT scaffold before decisions are
elected. If the scope is unclear, pick a mode first: `forge` (default) · `spec` · `slice` ·
`decide` · `evolve`.
