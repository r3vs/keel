---
description: Build a new project aligned from the first commit — invoke the greenfield-forge skill.
agent: researcher
---
Use the **greenfield-forge** skill — by name. Every host resolves a skill by name, so this command
never needs to know where the skill lives, and there is nothing here for two hosts to disagree
about.

Start at Phase 1 (frame: testable outcomes + open decisions + the threat-model pass) — do NOT
scaffold before decisions are elected. If the scope is unclear, pick a mode with me first:
`forge` (default) · `spec` · `slice` · `decide` · `evolve`.
